package com.game;import android.app.Activity;import android.content.Context;import android.content.Intent;import android.content.res.Resources;import android.graphics.PixelFormat;import android.os.Bundle;import android.os.Handler;import android.util.Log;import android.view.KeyEvent;import android.view.LayoutInflater;import android.view.View;import android.view.ViewGroup;import android.view.animation.AlphaAnimation;import android.widget.LinearLayout;public class LoadActivity extends Activity { 	@Override	protected void onCreate(Bundle savedInstanceState) {		super.onCreate(savedInstanceState);		if((getIntent().getFlags() & Intent.FLAG_ACTIVITY_BROUGHT_TO_FRONT) != 0)		{  			finish();			return;		}		getWindow().setFormat(PixelFormat.RGBA_8888);//		getWindow().addFlags(WindowManager.LayoutParams.FLAG_DITHER);		View view = LayoutInflater.from(this).inflate(getResIdByName(getBaseContext(), "layout", "logo"), (ViewGroup)getWindow().getDecorView(), false);		setContentView(view);		//setContentView(R.layout.logo);		LinearLayout ll = view.findViewById(getResIdByName(getBaseContext(), "id", "LinearLayout02"));		//LinearLayout ll = (LinearLayout) findViewById(R.id.LinearLayout02);	    AlphaAnimation alphaAnimation = new AlphaAnimation(0.0f, 1.0f);  	    alphaAnimation.setDuration(2000);  	    ll.startAnimation(alphaAnimation);  		new Handler().postDelayed(new Runnable() {			public void run() {				/* Create an Intent that will start the Main Activity. */				Intent mainIntent = new Intent(LoadActivity.this,						GameActivity.class);				LoadActivity.this.startActivity(mainIntent);				LoadActivity.this.finish();			}		}, 2000);	}		@Override      public boolean onKeyDown(int keyCode, KeyEvent event) {        if (keyCode == KeyEvent.KEYCODE_BACK ) {         	System.out.println("LoadActivity onKeyDown");        }        return false;    }	private int getResIdByName(Context context, String resourceType, String resourceName){		Resources resources = context.getResources();		int id = resources.getIdentifier(resourceName, resourceType, context.getPackageName());		if (0 == id){			Log.e("LoadActivity", "读取资源文件失败，resourceName:" + resourceName);		}		return id;	}}